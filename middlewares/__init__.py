"""
Middlewares init file, whenever you created new file,
directory or package inside ```middlewares``` package -
remember to include them here.

Middlewares package contains all middlewares used in this project.
"""

# from loader import dp
# from .throttling import ThrottlingMiddleware
#
#
# if __name__ == "middlewares":
#     dp.middleware.setup(ThrottlingMiddleware())
