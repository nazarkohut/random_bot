"""
Additional functions init file, whenever you created new file,
directory or package inside ```additional_functions``` package -
remember to include them here.

Additional functions package has different functions used within the project.

Create a package and include methods there in case the function is used for particular command only.
Consider adding a python file inside ```additional_functions```
package in case this method is used in more than one place.

Note: one file has one method; keep it consistent.
"""
from . import random_integer
from . import random_color
