"""
Random color init file, whenever you created new file,
directory or package inside ```random_color``` package -
remember to include them here.

Random color package has all functions needed for ```/random_color``` command.
Note: one file has one method; keep it consistent.
"""
from . import random_hex_code
