"""
Decorators init file, whenever you created new file,
directory or package inside ```decorators``` package -
remember to include them here.

Decorators package contains useful decorators.
Note: one file has one decorator; keep it consistent.
"""
from . import is_group
